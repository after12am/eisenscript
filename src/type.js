// TODO: could be possible to unify to symbols.js
var Type = {
  Background: 'background',
  Primitive: 'primitive',
  Rule: 'rule', // never used?
  Property: 'property' // never used?
};

if (module) {
  module.exports = Type;
}
